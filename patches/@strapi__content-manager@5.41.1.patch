diff --git a/dist/admin/services/documents.js b/dist/admin/services/documents.js
index 91c63173e66aab5c8abfaad1c779776b510c0d82..97707f36479f7d355a4978955b77267653db8ae5 100644
--- a/dist/admin/services/documents.js
+++ b/dist/admin/services/documents.js
@@ -65,7 +65,16 @@ const documentApi = api.contentManagerApi.injectEndpoints({
                             params
                         }
                     }),
-                invalidatesTags: (result, _error, { model })=>[
+                invalidatesTags: (result, error, { model })=>{
+                        // INTORG-796: a failed create never wrote anything — invalidating
+                        // here can trigger a refetch that wipes the user's in-progress
+                        // edits before they've fixed the validation error. Only invalidate
+                        // on success. (Mirrors the same guard already applied to
+                        // updateDocument/publishDocument in this patch.)
+                        if (error) {
+                            return [];
+                        }
+                        return [
                         {
                             type: 'Document',
                             id: `${model}_LIST`
@@ -78,7 +87,8 @@ const documentApi = api.contentManagerApi.injectEndpoints({
                         'RecentDocumentList',
                         'CountDocuments',
                         'UpcomingReleasesList'
-                    ],
+                    ];
+                    },
                 transformResponse: (response, meta, arg)=>{
                     /**
          * TODO v6
@@ -269,7 +279,14 @@ const documentApi = api.contentManagerApi.injectEndpoints({
                             params
                         }
                     }),
-                invalidatesTags: (_result, _error, { collectionType, model, documentId })=>{
+                invalidatesTags: (_result, error, { collectionType, model, documentId })=>{
+                    // INTORG-796: a failed publish never wrote anything — invalidating
+                    // here refetches the (unchanged) document and can cause <Form> to
+                    // re-sync away from the user's in-progress edits. Only invalidate
+                    // on success.
+                    if (error) {
+                        return [];
+                    }
                     return [
                         {
                             type: 'Document',
@@ -320,7 +337,13 @@ const documentApi = api.contentManagerApi.injectEndpoints({
                             params
                         }
                     }),
-                invalidatesTags: (_result, _error, { collectionType, model, documentId })=>{
+                invalidatesTags: (_result, error, { collectionType, model, documentId })=>{
+                    // INTORG-796: a failed save never wrote anything — invalidating here
+                    // refetches the (unchanged) document and can cause <Form> to re-sync
+                    // away from the user's in-progress edits. Only invalidate on success.
+                    if (error) {
+                        return [];
+                    }
                     return [
                         {
                             type: 'Document',
@@ -348,8 +371,13 @@ const documentApi = api.contentManagerApi.injectEndpoints({
                     try {
                         await queryFulfilled;
                     } catch  {
-                        // Rollback the optimistic update if there's an error
-                        patchResult.undo();
+                        // INTORG-796: do not undo the optimistic cache patch on error.
+                        // Undoing it reverts `getDocument`'s cached data to the pre-save
+                        // (stale) values; <Form>'s initialValues-sync effect then wipes
+                        // the user's in-progress edits back to that stale state, even
+                        // though the failed save never touched the database. Leaving the
+                        // optimistic (attempted) values in place keeps the form showing
+                        // what the user typed alongside the validation error.
                     }
                 },
                 transformResponse: (response, meta, arg)=>{
diff --git a/dist/admin/services/documents.mjs b/dist/admin/services/documents.mjs
index 4a7bec77421fd671c74b8976fd8be97940c0d5c7..f822c6c05010e4288596e6862304fcf2068c4ea4 100644
--- a/dist/admin/services/documents.mjs
+++ b/dist/admin/services/documents.mjs
@@ -63,7 +63,16 @@ const documentApi = contentManagerApi.injectEndpoints({
                             params
                         }
                     }),
-                invalidatesTags: (result, _error, { model })=>[
+                invalidatesTags: (result, error, { model })=>{
+                        // INTORG-796: a failed create never wrote anything — invalidating
+                        // here can trigger a refetch that wipes the user's in-progress
+                        // edits before they've fixed the validation error. Only invalidate
+                        // on success. (Mirrors the same guard already applied to
+                        // updateDocument/publishDocument in this patch.)
+                        if (error) {
+                            return [];
+                        }
+                        return [
                         {
                             type: 'Document',
                             id: `${model}_LIST`
@@ -76,7 +85,8 @@ const documentApi = contentManagerApi.injectEndpoints({
                         'RecentDocumentList',
                         'CountDocuments',
                         'UpcomingReleasesList'
-                    ],
+                    ];
+                    },
                 transformResponse: (response, meta, arg)=>{
                     /**
          * TODO v6
@@ -267,7 +277,14 @@ const documentApi = contentManagerApi.injectEndpoints({
                             params
                         }
                     }),
-                invalidatesTags: (_result, _error, { collectionType, model, documentId })=>{
+                invalidatesTags: (_result, error, { collectionType, model, documentId })=>{
+                    // INTORG-796: a failed publish never wrote anything — invalidating
+                    // here refetches the (unchanged) document and can cause <Form> to
+                    // re-sync away from the user's in-progress edits. Only invalidate
+                    // on success.
+                    if (error) {
+                        return [];
+                    }
                     return [
                         {
                             type: 'Document',
@@ -318,7 +335,13 @@ const documentApi = contentManagerApi.injectEndpoints({
                             params
                         }
                     }),
-                invalidatesTags: (_result, _error, { collectionType, model, documentId })=>{
+                invalidatesTags: (_result, error, { collectionType, model, documentId })=>{
+                    // INTORG-796: a failed save never wrote anything — invalidating here
+                    // refetches the (unchanged) document and can cause <Form> to re-sync
+                    // away from the user's in-progress edits. Only invalidate on success.
+                    if (error) {
+                        return [];
+                    }
                     return [
                         {
                             type: 'Document',
@@ -346,8 +369,13 @@ const documentApi = contentManagerApi.injectEndpoints({
                     try {
                         await queryFulfilled;
                     } catch  {
-                        // Rollback the optimistic update if there's an error
-                        patchResult.undo();
+                        // INTORG-796: do not undo the optimistic cache patch on error.
+                        // Undoing it reverts `getDocument`'s cached data to the pre-save
+                        // (stale) values; <Form>'s initialValues-sync effect then wipes
+                        // the user's in-progress edits back to that stale state, even
+                        // though the failed save never touched the database. Leaving the
+                        // optimistic (attempted) values in place keeps the form showing
+                        // what the user typed alongside the validation error.
                     }
                 },
                 transformResponse: (response, meta, arg)=>{
