# Server (generate with: bun -e "console.log('APP_KEYS=' + ...)" etc. - see cms/README.md)
APP_KEYS=...
API_TOKEN_SALT=...
ADMIN_JWT_SECRET=...
TRANSFER_TOKEN_SALT=...

# Strapi API
STRAPI_URL=http://localhost:1337
STRAPI_API_TOKEN=your-api-token

# Disable Git sync (local development only), or specify the repository path used by the Git sync script.
# STRAPI_DISABLE_GIT_SYNC=true
# STRAPI_GIT_SYNC_REPO_PATH=~/interledger.org-v5-staging