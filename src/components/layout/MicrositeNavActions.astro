---
import type { MenuItem } from '@/types/navigation'
import LanguageSwitcher from '@/components/shared/LanguageSwitcher.astro'
import { buildDeferredUmamiAttrs } from '@/utils'

interface Props {
  ctaButton?: MenuItem
}

const { ctaButton } = Astro.props
const { routeLocale } = Astro.locals

const ctaLinkClass =
  'self-center py-xl no-underline hover:underline transition-colors duration-150 max-desktop:hidden'

const ctaAttrs =
  ctaButton &&
  buildDeferredUmamiAttrs({
    pathname: Astro.url.pathname,
    lang: routeLocale,
    label: 'button_cta',
    baseComponent: 'menu',
    href: ctaButton.href || '#',
    linkText: ctaButton.label
  })
---

<div
  class="flex items-center gap-md desktop:col-start-3 desktop:col-end-4 desktop:justify-self-end max-desktop:flex-col max-desktop:items-start max-desktop:px-lg"
>
  {
    ctaButton && (
      <a
        class={ctaLinkClass}
        href={ctaButton.href || '#'}
        {...ctaAttrs}
        {...(ctaButton.openInNewTab
          ? { target: '_blank', rel: 'noopener noreferrer' }
          : {})}
      >
        {ctaButton.label}
      </a>
    )
  }
  <LanguageSwitcher />
</div>
