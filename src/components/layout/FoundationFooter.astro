---
import {
  translatePath,
  useTranslations,
  buildUmamiAttrs,
  getNavigation,
  localizeRoute,
  type UiKey
} from '@/utils'
import type { MenuGroup } from '@/types/navigation'
import Icon from '@/components/shared/Icon.astro'
import LinkButton from '@/components/shared/LinkButton.astro'
import InterledgerIcon from '@/components/logos/InterledgerIcon.astro'

const { routeLocale } = Astro.locals
const t = useTranslations(routeLocale)

const { mainMenu } = getNavigation('foundation', routeLocale)

const resourcesColumn: MenuGroup = {
  label: 'Resources',
  items: [
    { label: 'Terms of Service', href: '/terms-of-service' },
    { label: 'Privacy Policy', href: '/privacy-policy' },
    { label: 'Press & Media', href: '/press-and-media' },
    { label: 'FAQ', href: '/faq' }
  ]
}

const footerColumns = [...mainMenu, resourcesColumn]

const subscribeHref = translatePath(
  'foundation-pages',
  routeLocale,
  'subscribe'
)
const contactHref = translatePath('foundation-pages', routeLocale, 'contact')
const homeHref = localizeRoute('/', routeLocale)

const footerLink = (href: string, ariaLabel?: string, linkText?: string) =>
  buildUmamiAttrs({
    pathname: Astro.url.pathname,
    lang: routeLocale,
    section: 'footer',
    href,
    ariaLabel,
    linkText
  })

const footerExternalLinkIconClass = 'size-4 shrink-0 translate-y-[-1px]'

const currentPath = Astro.url.pathname.replace(/\/$/, '')
const isActive = (href: string | null | undefined) =>
  !!href && currentPath === localizeRoute(href, routeLocale).replace(/\/$/, '')

type SocialIconName =
  | 'youtube'
  | 'mastodon'
  | 'x'
  | 'github'
  | 'instagram'
  | 'linkedin'
  | 'slack'

const socialLinks: Array<{
  href: string
  icon: SocialIconName
  labelKey: UiKey
}> = [
  {
    href: 'https://www.youtube.com/@InterledgerFoundation',
    icon: 'youtube',
    labelKey: 'footer.youtube'
  },
  {
    href: 'https://interledger.social/about',
    icon: 'mastodon',
    labelKey: 'footer.mastodon'
  },
  { href: 'https://x.com/interledger', icon: 'x', labelKey: 'footer.x' },
  {
    href: 'https://github.com/interledger',
    icon: 'github',
    labelKey: 'footer.github'
  },
  {
    href: 'https://www.instagram.com/interledgerfoundation/',
    icon: 'instagram',
    labelKey: 'footer.instagram'
  },
  {
    href: 'https://www.linkedin.com/company/interledger-foundation/',
    icon: 'linkedin',
    labelKey: 'footer.linkedin'
  },
  {
    href: 'https://communityinviter.com/apps/interledger/interledger-working-groups-slack',
    icon: 'slack',
    labelKey: 'footer.slack'
  }
]
---

<footer
  data-component="FoundationFooter"
  class="bg-ice-mint-50"
  role="contentinfo"
>
  <div
    class="mx-auto max-w-wide min-w-[360px] px-lg tablet:px-xl desktop:px-3xl-tight"
  >
    <!-- Top: Subscribe | Contact | Social -->
    <div
      class="grid grid-cols-1 gap-3xl pt-4xl pb-3xl tablet:pt-3xl-tight tablet:gap-2xl desktop:pt-4xl desktop:grid-cols-6 desktop:gap-space-xl"
    >
      <!-- Subscribe -->
      <section
        aria-labelledby="footer-subscribe-heading"
        class="desktop:col-span-2"
      >
        <h5
          id="footer-subscribe-heading"
          class="mb-md text-h5-md desktop:text-h5-lg"
        >
          {t('footer.stay_up_to_date')}
        </h5>
        <p class="mb-xl max-w-[44ch] text-body-sm-standard text-neutral-75">
          {t('footer.newsletter_description')}
        </p>
        <LinkButton
          href={subscribeHref}
          size="lg"
          class="w-full tablet:w-auto"
          {...footerLink(subscribeHref, undefined, t('footer.subscribe'))}
        >
          {t('footer.subscribe')}
          <Icon slot="icon-right" name="arrow-right" />
        </LinkButton>
      </section>

      <!-- Contact -->
      <section
        aria-labelledby="footer-contact-heading"
        class="desktop:col-span-2"
      >
        <h5
          id="footer-contact-heading"
          class="mb-md text-h5-md desktop:text-h5-lg"
        >
          {t('footer.contact_us')}
        </h5>
        <p class="mb-xl max-w-[44ch] text-body-sm-standard text-neutral-75">
          {t('footer.contact_description')}
        </p>
        <LinkButton
          href={contactHref}
          variant="secondary"
          size="lg"
          class="w-full tablet:max-w-fit"
          {...footerLink(contactHref, undefined, t('footer.contact_us'))}
        >
          {t('footer.contact_us')}
        </LinkButton>
      </section>

      <!-- Social Links -->
      <section
        aria-labelledby="footer-social-heading"
        class="desktop:col-span-2"
      >
        <h5
          id="footer-social-heading"
          class="mb-md text-h5-md desktop:text-h5-lg"
        >
          {t('footer.social_links')}
        </h5>
        <ul
          class="mb-0 mt-space-xs mx-0 flex list-none flex-wrap gap-md p-0 tablet:gap-xl
          desktop:gap-lg"
          role="list"
        >
          {
            socialLinks.map((link) => (
              <li>
                <LinkButton
                  href={link.href}
                  variant="secondary"
                  size="lg"
                  iconOnly
                  external
                  aria-label={t(link.labelKey)}
                  {...footerLink(link.href, t(link.labelKey))}
                >
                  <Icon name={link.icon} class="size-6" />
                </LinkButton>
              </li>
            ))
          }
        </ul>
      </section>
    </div>

    <!-- Navigation columns -->
    <nav
      aria-label={t('footer.nav_label')}
      class="border-t border-neutral-50 py-3xl"
    >
      <ul
        class="m-0 grid list-none grid-cols-1 gap-3xl p-0 tablet:grid-cols-3 desktop:grid-cols-6 desktop:gap-space-xl"
        role="list"
      >
        {
          footerColumns.map((group, groupIndex) => {
            const groupHeadingId = `footer-nav-group-${groupIndex}`
            return (
              <li class="min-w-0">
                <h6
                  id={groupHeadingId}
                  class="mb-xl text-h4 tablet:text-body-lg-standard-md font-normal uppercase tracking-wide text-neutral-900"
                >
                  {group.label}
                </h6>

                {group.items && group.items.length > 0 && !group.subGroups && (
                  <ul
                    class="m-0 flex list-none flex-col gap-xl tablet:gap-md p-0"
                    role="list"
                    aria-labelledby={groupHeadingId}
                  >
                    {group.items.map((item) => (
                      <li>
                        <LinkButton
                          variant="ghost"
                          href={item.href ?? '#'}
                          class={`whitespace-nowrap${item.openInNewTab ? ' gap-space-3xs' : ''}`}
                          aria-current={
                            isActive(item.href) ? 'page' : undefined
                          }
                          external={item.openInNewTab}
                          {...footerLink(
                            item.href ?? '#',
                            undefined,
                            item.label
                          )}
                        >
                          {item.label}
                          {item.openInNewTab && (
                            <Icon
                              slot="icon-right"
                              name="external-link"
                              class={footerExternalLinkIconClass}
                            />
                          )}
                        </LinkButton>
                      </li>
                    ))}
                  </ul>
                )}

                {group.subGroups && group.subGroups.length > 0 && (
                  <div class="flex flex-col gap-xl tablet:flex-wrap desktop:flex-col desktop:gap-space-m">
                    {group.subGroups.map((sub, subIndex) => {
                      const subHeadingId = `${groupHeadingId}-sub-${subIndex}`
                      return (
                        <div class="flex flex-col gap-md">
                          <p
                            id={subHeadingId}
                            class="mb-2 text-caption font-normal uppercase tracking-wide text-neutral-100 whitespace-nowrap tablet:text-neutral-900"
                          >
                            <span class="border-b border-neutral-100 pb-space-3xs tablet:border-neutral-900">
                              {sub.label}
                            </span>
                          </p>
                          <ul
                            class="m-0 flex list-none flex-col gap-xl tablet:gap-md p-0"
                            role="list"
                            aria-labelledby={subHeadingId}
                          >
                            {sub.items.map((item) => (
                              <li>
                                <LinkButton
                                  variant="ghost"
                                  href={item.href ?? '#'}
                                  class={`whitespace-nowrap${item.openInNewTab ? ' gap-space-3xs' : ''}`}
                                  aria-current={
                                    isActive(item.href) ? 'page' : undefined
                                  }
                                  external={item.openInNewTab}
                                  {...footerLink(
                                    item.href ?? '#',
                                    undefined,
                                    item.label
                                  )}
                                >
                                  {item.label}
                                  {item.openInNewTab && (
                                    <Icon
                                      slot="icon-right"
                                      name="external-link"
                                      class={footerExternalLinkIconClass}
                                    />
                                  )}
                                </LinkButton>
                              </li>
                            ))}
                          </ul>
                        </div>
                      )
                    })}
                  </div>
                )}
              </li>
            )
          })
        }
      </ul>
    </nav>

    <!-- Bottom: copyright, legal links, logo -->
    <div class="border-t border-neutral-50 pt-3xl pb-3xl-tight text-center">
      <p class="mb-3xl text-body-sm-standard text-neutral-75">
        {t('footer.copyright')}
      </p>

      <a
        href={homeHref}
        aria-label={t('footer.logo_home')}
        class="inline-block rounded-sm transition-opacity hover:opacity-80 focus-visible:ring-2 focus-visible:ring-current focus-visible:ring-offset-4 focus-visible:outline-none"
        {...footerLink(homeHref, t('footer.logo_home'), t('footer.logo_home'))}
      >
        <InterledgerIcon class="h-10 w-10" />
      </a>
    </div>
  </div>
</footer>
