---
import PdfEmbed from '@/components/shared/PdfEmbed.astro'

interface Props {
  file?: { url: string }
  externalUrl?: string
  label?: string
}

const { file, externalUrl, label } = Astro.props

const url = file?.url ?? externalUrl
---

{url && <PdfEmbed url={url} label={label} />}
