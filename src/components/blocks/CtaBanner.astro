---
import { buildUmamiAttrs } from '@/utils'

interface Props {
  heading: string
  text?: string
  primaryButtonText?: string
  primaryButtonLink?: string
  secondaryButtonText?: string
  secondaryButtonLink?: string
  backgroundColor?: 'primary' | 'secondary' | 'light' | 'dark'
  umamiContext?: string
}

const {
  heading,
  text,
  primaryButtonText,
  primaryButtonLink,
  secondaryButtonText,
  secondaryButtonLink,
  backgroundColor = 'primary',
  umamiContext
} = Astro.props

const { routeLocale } = Astro.locals
const umamiBase = {
  page: umamiContext,
  pathname: Astro.url.pathname,
  lang: routeLocale,
  section: 'cta' as const
}

const bgClasses = {
  primary: 'bg-gradient-primary text-white',
  secondary: 'bg-neutral-100',
  light: 'bg-neutral-50',
  dark: 'bg-black text-white'
}

const headingClasses = {
  primary: 'text-white',
  secondary: 'text-primary',
  light: 'text-primary',
  dark: 'text-white'
}

const baseButtonClasses =
  'inline-block rounded-full py-lg px-2xl font-medium no-underline transition-all duration-200'

const primaryButtonClasses = {
  primary: `${baseButtonClasses} bg-white text-primary hover:opacity-90`,
  secondary: `${baseButtonClasses} bg-primary text-white`,
  light: `${baseButtonClasses} bg-primary text-white`,
  dark: `${baseButtonClasses} bg-primary text-white`
}

const secondaryButtonClasses = {
  primary: `${baseButtonClasses} bg-transparent border-2 border-white text-white hover:bg-white hover:text-primary`,
  secondary: `${baseButtonClasses} bg-transparent border-2 border-primary text-primary`,
  light: `${baseButtonClasses} bg-transparent border-2 border-primary text-primary`,
  dark: `${baseButtonClasses} bg-transparent border-2 border-white text-white`
}
---

<section class={`py-4xl px-2xl text-center ${bgClasses[backgroundColor]}`}>
  <div class="max-w-narrow mx-auto">
    <h2
      class={`text-[2rem] font-semibold mb-xl ${headingClasses[backgroundColor]}`}
    >
      {heading}
    </h2>
    {text && <p class="text-[1.125rem] mb-2xl opacity-90">{text}</p>}
    {
      (primaryButtonText || secondaryButtonText) && (
        <div class="flex flex-wrap justify-center gap-xl">
          {primaryButtonText && primaryButtonLink && (
            <a
              href={primaryButtonLink}
              class={primaryButtonClasses[backgroundColor]}
              {...buildUmamiAttrs({
                ...umamiBase,
                href: primaryButtonLink,
                linkText: primaryButtonText
              })}
            >
              {primaryButtonText}
            </a>
          )}
          {secondaryButtonText && secondaryButtonLink && (
            <a
              href={secondaryButtonLink}
              class={secondaryButtonClasses[backgroundColor]}
              {...buildUmamiAttrs({
                ...umamiBase,
                href: secondaryButtonLink,
                linkText: secondaryButtonText
              })}
            >
              {secondaryButtonText}
            </a>
          )}
        </div>
      )
    }
  </div>
</section>
