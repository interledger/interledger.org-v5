---
import VideoEmbed from '@/components/shared/VideoEmbed.astro'

interface Props {
  file?: { url: string }
  externalUrl?: string
  title: string
}

const { file, externalUrl, title } = Astro.props

const url = file?.url ?? externalUrl
---

{url && title && <VideoEmbed url={url} title={title} />}
