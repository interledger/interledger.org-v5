---
// Renders a plain text string as a stack of <p> elements, one per non-empty line.

interface Props {
  text: string | null
}

const { text } = Astro.props
const paragraphs = text?.split(/\r?\n/).filter(Boolean) ?? []
---

{paragraphs.map((para) => <p class="mb-6">{para}</p>)}
