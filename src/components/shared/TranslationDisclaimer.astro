---
interface Props {
  isVisible?: boolean
  style?: 'foundation' | 'summit'
}

const { routeLocale } = Astro.locals
const { isVisible = false, style = 'foundation' } = Astro.props
const spanishDisclaimer = 'Esta página aún no está disponible en español.'

const styleClass = {
  foundation: 'text-primary',
  summit: 'text-white/80'
}[style]
---

{
  isVisible && routeLocale === 'es' && (
    <section class="py-xl">
      <div class="mx-auto w-full max-w-wide px-2xl">
        <p
          class={`translation-disclaimer max-w-fit px-xl py-s text-body-sm-standard tablet:text-h5-md desktop:text-h5-lg ${styleClass}`}
        >
          {spanishDisclaimer}
        </p>
      </div>
    </section>
  )
}
