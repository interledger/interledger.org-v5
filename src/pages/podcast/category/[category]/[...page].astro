---
import type { Page, GetStaticPaths } from 'astro'
import { paginatePodcastEpisodesByTerm } from '@/utils'
import type { PodcastPageData } from '@/utils'
import type { Podcast } from '@/types/podcast'
import PodcastPage from '@/components/pages/PodcastPage.astro'

type Props = {
  page: Page<Podcast>
  podcastPage: PodcastPageData
  isFallback: boolean
  allTerms: string[]
  selectedTerm?: string
}

export const getStaticPaths: GetStaticPaths = async ({ paginate }) => {
  return paginatePodcastEpisodesByTerm({ paginate, lang: 'en' })
}
---

<PodcastPage {...Astro.props} />
