---
import FoundationContentPage from '@/components/pages/FoundationContentPage.astro'
import { type Locale, HOME_CONTENT_SLUG } from '@/utils'

const pageLang: Locale = 'en'
---

<FoundationContentPage slug={HOME_CONTENT_SLUG} contentLocale={pageLang} />
