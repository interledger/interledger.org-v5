---
import type { Page, GetStaticPaths } from 'astro'
import { paginatePostsByTag } from '@/utils/tagFilter'
import FoundationBlogListingPage from '@/components/blog/FoundationBlogListingPage.astro'

type Props = {
  page: Page
  allTags: string[]
  selectedTag: string
}

export const getStaticPaths: GetStaticPaths = async ({ paginate }) => {
  const paths = await paginatePostsByTag({
    paginate,
    collection: 'foundation-blog',
    lang: 'en'
  })
  return paths
}

const { page, allTags, selectedTag } = Astro.props
---

<FoundationBlogListingPage
  page={page}
  allTags={allTags}
  selectedTag={selectedTag}
/>
