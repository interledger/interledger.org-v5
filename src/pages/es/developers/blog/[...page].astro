---
import { paginateAllPosts } from '@/utils/tagFilter'
import type { GetStaticPaths, Page } from 'astro'
import DevelopersBlogListingPage from '@/components/blog/DevelopersBlogListingPage.astro'

type Props = {
  page: Page
  allTags: string[]
}

export const getStaticPaths: GetStaticPaths = async ({ paginate }) => {
  const paths = await paginateAllPosts({
    paginate,
    collection: 'developers-blog',
    lang: 'es'
  })
  return paths
}

const { page, allTags } = Astro.props
---

<DevelopersBlogListingPage
  page={page}
  allTags={allTags}
  blogPath="/es/developers/blog"
  developersHomePath="/es/developers"
  foundationBlogPath="/es/blog"
/>
