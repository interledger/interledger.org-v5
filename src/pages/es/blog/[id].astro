---
import FoundationBlogPostPage from '@/components/pages/FoundationBlogPostPage.astro'
import { getLocalizedPaths } from '@/utils/static-paths'

export async function getStaticPaths() {
  return getLocalizedPaths('foundation-blog', 'es', 'id')
}

const { slug, locale } = Astro.props
---

<FoundationBlogPostPage slug={slug} locale={locale} />
