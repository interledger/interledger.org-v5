{
  "collectionName": "components_blocks_carousels",
  "info": {
    "displayName": "Logo Carousel",
    "icon": "images",
    "description": "Partner logo carousel (image + per-logo alt). After changing the logos field shape, rehydrate existing rows with sync:mdx via workflow_dispatch — a normal cms rebuild does not re-sync content."
  },
  "options": {},
  "attributes": {
    "heading": {
      "type": "string",
      "pluginOptions": {
        "i18n": {
          "localized": true
        }
      }
    },
    "accessibilityLabel": {
      "type": "string",
      "required": true,
      "pluginOptions": {
        "i18n": {
          "localized": true
        }
      }
    },
    "logos": {
      "type": "component",
      "repeatable": true,
      "component": "blocks.carousel-logo",
      "required": true,
      "min": 1,
      "pluginOptions": {
        "i18n": {
          "localized": true
        }
      }
    }
  }
}
