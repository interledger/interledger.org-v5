/**
 * Serialize blocks.hackathon-animation → MDX.
 *
 * Fieldless marker block — always emits the same self-closing tag.
 */
export function serialize(): string {
  return '<HackathonAnimation />'
}
