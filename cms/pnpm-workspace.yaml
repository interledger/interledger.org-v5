packages: []

onlyBuiltDependencies:
  - '@swc/core'
  - better-sqlite3
  - core-js-pure
  - esbuild
  - sharp

overrides:
  # Pinned to the version @strapi/admin 5.41.1 itself renders with — the admin
  # notice bar imports it directly, so `@strapi/design-system` is also a direct
  # dependency in package.json. Bump both together, and only alongside Strapi.
  '@strapi/design-system': 2.2.0
  '@strapi/icons': 2.2.0

patchedDependencies:
  '@strapi/content-manager@5.41.1': ../patches/@strapi__content-manager@5.41.1.patch
